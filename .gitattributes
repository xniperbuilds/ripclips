# Normalize line endings across platforms
* text=auto eol=lf

# Windows scripts keep CRLF
*.ps1 text eol=crlf

# Unix shell scripts must stay LF (or they break on macOS/Linux)
*.sh text eol=lf

# Treat media as binary
*.mp4 binary
*.png binary
*.jpg binary
