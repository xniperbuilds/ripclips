# RipClips Python dependencies
# (ffmpeg is a system binary and is installed by setup.ps1 / setup.sh, not pip)

yt-dlp>=2024.1.0        # transcript + video download
PyYAML>=6.0             # config.yaml parsing
opencv-python>=4.8.0    # general-mode auto face tracking (step 5)
